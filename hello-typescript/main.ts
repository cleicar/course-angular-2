var minhaVar = "minha variavel";

function minhaFun(x, y) {
	return x + y;
}

// ECS 6 ou ECS 2015
let num  = 2;
const PI = 3.14;

var numeros = [1, 2, 3];

numeros.map(function(valor) {
	return valor * 2;
});

// ARROW FUNCTION
numeros.map(valor => valor * 2);

// CLASSES
class Matematica{
	soma(x,y){
		return x + y;
	}
}

// TIPAGEM DE VARIAVEL
let n1: string = 'teste';
